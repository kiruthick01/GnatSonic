; Phase 2 (ROADMAP.md §7) — RX node (INMP441 I2S mic). Not started: awaiting
; Phase 0 measured f0/f1 before any protocol-specific code is written.

[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
